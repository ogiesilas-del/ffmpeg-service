{
  "phases": {
    "setup": {
      "nixPkgs": ["python39", "python39Packages.pip", "ffmpeg", "git"]
    },
    "install": {
      "cmds": ["pip3 install --break-system-packages -r requirements.txt"]
    },
    "build": null
  },
  "start": {
    "cmd": "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}
